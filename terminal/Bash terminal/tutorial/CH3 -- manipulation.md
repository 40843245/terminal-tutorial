# CH3 -- manipulation
## objectives
You will know how to 

  + manipulate string

## CH3-1 -- manipulate string
To get length of string,

use `$[#<variable-name>]`.

`$[<variable-name>]` expands the variable

`#`:`#<variable-name>` get the length of `<variable-name>`
